# UI and Interaction Standards

- Use `shadcn/ui` components for all standard interface elements.
- Use `Tailwind CSS` for all styling.
- All forms MUST use `e.preventDefault()` to prevent unwanted page refreshes.
- File uploads MUST use a `react-dropzone` wrapper with visual progress indicators.
- Interactive elements must have unique, descriptive IDs for better accessibility and testing.
